/// @description Inserir descrição aqui
// Você pode escrever seu código neste editor

canshoot = true;
vida = 3;
ivencibility = false;
coins = 0;
timer = 60;
lastRoom = 0;
randomize();

/*
function spawners(){
	var hpos = random_range(320,1266);
	var vpos = random_range(100,668);
	instance_create_layer(hpos,vpos,"Instances",Obj_spawner);
}

if (room != Rm_store){
	for(i=0;i<5;i++){
		spawners();
	}
}

/*

var hpos = random_range(320,1266);
var vpos = random_range(100,668);
instance_create_layer(hpos,vpos,"Instances",Obj_spawner);

hpos = random_range(320,1266);
vpos = random_range(100,668);
instance_create_layer(hpos,vpos,"Instances",Obj_spawner);

hpos = random_range(320,1266);
vpos = random_range(100,668);
instance_create_layer(hpos,vpos,"Instances",Obj_spawner);

hpos = random_range(320,1266);
vpos = random_range(100,668);
instance_create_layer(hpos,vpos,"Instances",Obj_spawner);

hpos = random_range(320,1266);
vpos = random_range(100,668);
instance_create_layer(hpos,vpos,"Instances",Obj_spawner);

*/
